// firebaseConfig.js (si divides la configuración)
import { initializeApp } from 'firebase/app';

const firebaseConfig = {
  // ... (misma configuración que en config.js)
};

export const firebaseApp = initializeApp(firebaseConfig);